import { defineConfig } from "vite";

export default defineConfig({
  // For project pages: https://<user>.github.io/house/
  // If repo name differs, replace /house/ with your repo name.
  base: "/game-house/"
});
